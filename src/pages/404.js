import { Container, Typography } from '@mui/material';

const Error = () => (
  // eslint-disable-next-line react/react-in-jsx-scope
  <Container>
    {/* eslint-disable-next-line react/react-in-jsx-scope */}
    <Typography>Perdu ???</Typography>
  </Container>
);

export default Error;
